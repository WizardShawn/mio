{
  "name": "mio-avatar-bundler",
  "version": "0.1.0",
  "private": true,
  "description": "Bundles the desktop Three.js + three-vrm avatar scene into a single JS file that the Android WebView loads via file:///android_asset/avatar/main.bundle.js. Invoked from the `bundleMioAssets` Gradle task; not intended to be run standalone in production.",
  "type": "module",
  "scripts": {
    "build": "node bundle.mjs"
  },
  "dependencies": {
    "@pixiv/three-vrm": "^3.4.0",
    "@pixiv/three-vrm-animation": "^3.4.0",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
