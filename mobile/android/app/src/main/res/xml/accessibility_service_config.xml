<?xml version="1.0" encoding="utf-8"?>
<!-- M-5.4 — Mio's foreground-app accessibility service.

     PRIVACY POSTURE: this config reads the foreground app's package
     name ONLY. `canRetrieveWindowContent` is false and no
     `flagRetrieveInteractiveWindows` is requested, so window text and
     on-screen content are never accessible to the service. The event
     stream is narrowed to window-state changes. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description" />
