<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash + system bar baseline. The Compose layer overrides the rest -->
    <style name="Theme.MioMobile" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/mio_bg</item>
        <item name="android:navigationBarColor">@color/mio_bg</item>
        <item name="android:windowBackground">@color/mio_bg</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
